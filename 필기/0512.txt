<<배열
같은 종류의 데이터를 모아놓은 것
1. 배열생성
$arr=array();   //배열생성
$arr[0]="apple";
$arr[1]="banana";
$arr[0]="melon";

$arr2=array("apple","banana","melon");

2. 연관배열
1)형식:$배열이름= array(키=>값, 키=>값, 키=>값.....);
-----------------------------------------------------------
<<함수
형식: 
function 함수이름 (매개변수.............){
실행될 코드
}
